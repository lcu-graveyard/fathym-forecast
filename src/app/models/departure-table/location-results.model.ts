import { LocationAddress } from './location-address.model';
import { LocationPosition } from './location-position.model';

export class LocationResults {
    public address: LocationAddress;
    public position: LocationPosition;
}